<?xml version="1.0" encoding="utf-8"?>
<merge
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto"
        tools:layout_height="wrap_content"
        tools:layout_width="match_parent"
        tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/shiftImageView"
            android:layout_width="80dp"
            android:layout_height="60dp"
            android:scaleType="fitXY"
            app:layout_constraintStart_toStartOf="parent"
            tools:background="@color/colorAccent"
    />

    <TextView
            android:id="@+id/shiftStartTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/shiftImageView"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Start: May 3, 2018"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
    />

    <TextView
            android:id="@+id/shiftEndTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/shiftImageView"
            app:layout_constraintTop_toBottomOf="@id/shiftStartTime"
            tools:text="End: May 3, 2018"
            android:layout_marginStart="10dp"
    />

</merge>